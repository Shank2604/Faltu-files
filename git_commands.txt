git init
git status
git add testing.py
git rm --cached testing.py -- removed from staged.
git commit -m "Add testing file"
git push -- push the files from local to Github repo.
git push origin master -- push files to master branch
git remote -v -- to chech our url of repo
git remote add origin <url> -- to get our repo from Github
git remote set-url origin <url> -- to change our settings to push code in master instead of main
git clone <url> -- to get anyones full repo on your local editor 

Pull cmd checking.

git pull origin  master -- to get changed code from github to local.

git branch -- to list current and all branches.
git branch <name> -- to create new branch
git switch <name> -- to swotch from one branch to another